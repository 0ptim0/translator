{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "cd ${config:nuttx_path} && cp .config ${workspaceFolder}/.config && make apps_clean && make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Configure",
            "type": "shell",
            "command": "cd ${config:nuttx_path} && make distclean && ./tools/configure.sh nucleo-f4x1re:f401-nsh",
        },
        {
            "label": "Flash",
            "type": "shell",
            "command": "/usr/local/bin/openocd -s ${config:openocd_path}/scripts -f board/st_nucleo_f4.cfg -c \"program ${config:nuttx_path}/nuttx verify reset exit\""
        },
    ]
}
